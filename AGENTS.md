## Learned User Preferences

- For French cover letters, prefer a natural, direct, human register over stiff or AI-sounding prose.
- Structure cover letters with an early value or role hook, concrete proof and metrics, and optional short employer-specific tailoring; complement the CV instead of repeating it as a credential list.
- Position the profile around **platform and graphics** (header `\position` and letters: e.g. English “Software Engineer --- Platform and graphics”, French “Ingénieur logiciel --- Plateforme et infographie”); keep breadth (backend, security, cloud, teaching, open source) in body copy without leading with a long CI/CD tool chain.
- When mentioning teaching, lead with the role or outcome and put course identifiers such as LOG8100 in parentheses rather than opening with the course code alone.
- When merging user-supplied French prose into LaTeX, keep tailoring macros (`\RecipientName`, `\RoleTitle`, `\WhyCompany`, `\CompanyName`, `\LetterSubject` where used) and fix typography (spaces around names like uml-mcp, hyphenation, no duplicated opening hooks).
- On static HTML CV pages (`index-en.html` / `index-fr.html`), **omit public cover-letter links** so the page stays PDF- and contact-oriented. For **About**, user may combine profile + education in one bullet (generalist platform/graphics wording, EN/FR parity) and **remove open-source maintainer copy from About** while keeping optional **github-profile-stats** in Contact; OSS credibility may still appear in Selected projects or per-repo star badges.
- For **Tech Stack** on `index-en.html` / `index-fr.html` and `cv-en/latex/skills.tex` / `cv-fr/latex/skills.tex`, keep EN/FR parity; split **CI/CD & Pipelines** from **Cloud / Platform**, **Testing / Quality** from **Observability**, and use separate **Intermediate** / **Intermédiaire** rows per tool when the user corrects level (e.g. Blender, CUDA, OpenCV, C#, Android, Vue, GraphQL, CSS, .NET, Azure DevOps, Terraform, Sentry, Serilog). Confirm HTML-only vs LaTeX sync per task—the user often wants both aligned after HTML changes.
- **Selected projects**: HTML uses collapsible groups (**Personal projects and maintained open source** / **Contributions and collaborative work**, with **Other accepted contributions** for minor OSS); flagship **uml-mcp** card (linked title, ★ 83 badge) with vertical inline SVG (`viewBox` 148×192, step badges 1–4; EN **UML diagram**, FR **Diagramme UML**); keep SVG inline (no Mermaid.js); simple copy (Cursor MCP + ChatGPT Custom GPT link); sync from `assets/uml-mcp-card-flow.svg` and `uml-mcp-card-flow-fr.svg`; no **plantumlapi** in copy; **GraphQuon 2024** links to `https://graphquon.github.io/` (not the Toronto series page); homelab card may lead with **MediaBoxDockerCompose** and secondary **install scripts** link; **project-title** is one linked repo name only (no `· NuGet` / `· upstream` / `· docs` in the title row—extra links in impact or tags).
- **Other accepted contributions** (HTML): keep lean with one `<li>` per secondary repo and EN/FR parity; do not repeat flagship project-card narratives (e.g. ipc-toolkit friction work stays on the ipc-toolkit card); put related work on the owning card (e.g. TrueMap/surfalize on **TrueMapData**) instead of a duplicate list line.
- When updating GitHub profile stars on the index, refresh the optional contact **github-profile-stats** row (contributions, stars, forks) from figures the user supplies; do not add OSS maintainer lines to About unless asked. Repo-level badges (e.g. uml-mcp ★ 83) stay separate from profile totals.
- **Education thesis** (HTML + LaTeX): humanize without semicolons; use **Thesis:** / **Mémoire :** with a comma and **with** / **avec** for friction and plural **textures**; keep EN/FR parity in `index-en.html`, `index-fr.html`, and `cv-en/latex/education.tex` / `cv-fr/latex/education.tex`.
- **About + AEO on static CV**: `about-en.html` / `about-fr.html` from `data/aeo.yaml` via `generate_aeo_content.py`—**h1 and short lead at top** (hero + summary cards), **nested FAQ** `<details class="aeo-faq-item">` (first open), section toggles with `css/cv-about-page.css` (About-only; `css/cv-aeo.css` is for CV index hidden lead). On `index-en.html` / `index-fr.html`, full AEO snippet at **end of `<main>`** with **`.aeo-agent-only`**; About section intro links to About pages.

## Learned Workspace Facts

- **LaTeX CVs + build**: Main files `cv-en/resume.tex` and `cv-fr/resume.tex` (Russell `russell.cls`, fonts under `cv-en/fonts/` and `cv-fr/fonts/`). Sections in `cv-en/latex/` and `cv-fr/latex/`. Docker: `python build_cv.py build -l en|fr` or `--all`; local: `latexmk -xelatex resume.tex`. See `docs/development/cv-compilation.md`. GitHub Actions use Python **3.14** (`docs/development/github-actions.md`). Local smoke: `scripts\ci\test-pipeline-local.ps1` (`-SkipCompile` for deploy-only).
- **Cover letters**: `letters/en/cover-letter.tex` and `letters/fr/cover-letter.tex` (Russell + `\fontdir`); `latexmk -xelatex cover-letter.tex` from `letters/en` or `letters/fr`. Tailoring: `\RecipientName`, `\CompanyName`, `\LetterSubject`, `\RoleTitle`, optional `\WhyCompany`. `\recipient{\RecipientName}{\ifdefempty{\CompanyName}{\vphantom{A}}{\CompanyName}}` avoids empty address breaks. Deploy does not copy letter PDFs to `_site/letters/`; `compile-cv.yml` may still build them locally.
- **Static HTML CV**: `index-en.html`, `index-fr.html`, `index.html` (sync: `python scripts/html/sync_index_html.py`); **`index-en.htm`** → `index-en.html` (`vercel.json`). **Site nav on CV**: **Blog** pill only (About lives on `about-*.html`). **About nav**: Blog | About | **Home** (no EN|FR language pill). Grouped **Selected projects** + lean **Other accepted contributions**; optional **Soft Skills** / **Compétences relationnelles** per job. Cal.com `https://cal.com/antoine-boucher-dev/30min`; GitHub/LinkedIn `href`s literal `https://` (not `&#x2F;`). **ClustrMaps** in page **body**, **hidden by default** (load on toggle). **particles.js** `defer` + skip when `prefers-reduced-motion: reduce`. uml-mcp flow **inline SVG** (GitHub Pages does not copy `assets/`). **Tech Stack**: Testing/Quality and Observability rows; CI/CD vs Cloud categories.
- **About / AEO pages**: `about-en.html` / `about-fr.html` generated from `data/aeo.yaml` (`python scripts/build/generate_aeo_content.py --all`); FAQPage JSON-LD + `Person.knowsAbout`; About layout/styles in `css/cv-about-page.css`. CV pages get AEO lead/freshness patches from the same script (`docs/development/aeo-tracker.md`).
- **Certifications (HTML + LaTeX)**: Credly UUIDs for AWS CCP and Cloud Quest tracks; QcES via Sertifier; GDPR/RGPD plain text. HTML may use **one compact `<li>`** for all six Cloud Quest Credly links. LaTeX: `cv-en/latex/certifications.tex` and `cv-fr/latex/certifications.tex`; no duplicate Credentials row in `skills.tex`.
- **Hugo vs CV data**: Portfolio/posts under `hugo/` (`docs/development/hugo-navigation.md`). **Hugo blog stays separate**—`data/cv.yaml` drives CV/LaTeX skills only, not Hugo Markdown posts.
- **Agent-ready**: Source `hugo/static/`; promote in `.github/workflows/deploy.yml`. Cloudflare orange-cloud cannot use `hugo/static/_headers` (`docs/development/agent-ready-baseline.md`). Post-deploy: `scripts\verify-agent-ready-production.ps1` (use **`-SitePrefix /CV`** for `https://antoineboucher.info/CV/...`).
- **Root sitemap + Hugo SEO**: `scripts/deploy/merge_root_sitemap.py` in `deploy.yml` (flatten Hugo `sitemapindex` under `blog/`). Post-build: `python scripts/verify/verify_deploy_build.py --site-dir _site --repo CV`. **`HUGO_ENV=production`** for canonical/OG/Twitter.
- **Machine-readable resume**: Canonical `data/resume.en.json` / `data/resume.fr.json`. `scripts/build/render_resume_md.py --all` → `resume.md` / `resume-fr.md`; deploy copies `resume.json` / `resume-fr.json`. English default (`x-default`). Update JSON first, then scripts, then HTML/LaTeX.
- **LaTeX skills from YAML**: `data/cv.yaml` → `cv-en/latex/skills.tex` / `cv-fr/latex/skills.tex` via `scripts/build/generate_cv.py` (CI: `--check`). Schema `data/cv.schema.json`; `scripts/verify/validate_cv_data.py`; EN/FR HTML parity `scripts/verify/check_en_fr_parity.py`. Experience/projects still hand-edited until migrated.
- **Static HTML CSS**: `css/cv-main.css`, `css/cv-print.css` (`scripts/html/extract_cv_css.py` from `index-en.html`); About pages in `css/cv-about-page.css`; CV index AEO snippet in `css/cv-aeo.css`.
- **CI quality**: `.github/workflows/quality.yml` (codespell, html-validate, validate, EN/FR parity, `generate_cv.py --check`), `lighthouse.yml`, blocking Lychee in `deploy.yml`, `scripts/verify/verify_meta_descriptions.py`, `scripts/verify/verify_cv_pdf_text.py` in `compile-cv.yml`. Local: `scripts/ci/run-tests.ps1`, `scripts/ci/run-act.ps1`, `scripts/ci/test-pipeline-local.ps1`. Legacy PDFs in `archive/pdf/`. Optional `pre-commit` per `.pre-commit-config.yaml`.
- **Continual learning + hook state**: Transcripts under `C:\Users\antoi\.cursor\projects\e-cv\agent-transcripts\` (exclude `subagents/`). Index: `.cursor/hooks/state/continual-learning-index.json`. `/.cursor/hooks/state/` is gitignored—`git rm --cached -r .cursor/hooks/state/` if tracked.
